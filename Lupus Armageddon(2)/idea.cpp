/*
Instead of generating legal moves, what about generating legal POSITIONS.
Steps:

1. Create a variable int WhiteToMove = 0;
2. if(WhiteToMove == 0) {
shift any occupied bit a legal number of squares
};
*/
